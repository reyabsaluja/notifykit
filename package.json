{
  "name": "notifykit-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun run --cwd packages/core build && bun run --cwd packages/drizzle-adapter build && bun run --cwd packages/react build && bun run --cwd packages/cli build && bun run --cwd packages/resend build && bun run --cwd packages/create-app build",
    "example": "bun run --cwd examples/basic start",
    "example:drizzle": "bun run --cwd examples/drizzle start",
    "example:handler": "bun run --cwd examples/handler start"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/bun": "^1.1.13"
  }
}
